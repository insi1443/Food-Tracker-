# Copy this file to ".env" and paste your real key after the "=" sign.
# Get a free key in ~30 seconds at: https://fdc.nal.usda.gov/api-key-signup.html
# The real .env file is git-ignored, so your key never gets pushed to GitHub.
USDA_API_KEY=your_api_key_here
